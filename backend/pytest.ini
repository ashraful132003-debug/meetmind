[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --tb=short
asyncio_mode = auto
# Pin this explicitly: pytest-asyncio warns when it is unset and plans to change
# the default, which would silently alter fixture lifetimes on a future upgrade.
asyncio_default_fixture_loop_scope = function
